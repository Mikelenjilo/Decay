import 'package:flutter/material.dart';

void main() {
  runApp(Decay());
}

class Decay extends StatelessWidget {
  const Decay({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
